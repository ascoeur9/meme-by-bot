# inbox/grok

**KO:** 여기에 밈 JSON 배열 파일을 넣으세요. 각 파일은 밈 객체 배열입니다. `npm run merge-inbox`(또는 GitHub Action)가 스키마 검증 후 `data/memes.json`에 병합하고, 처리된 inbox JSON은 삭제됩니다.

**EN:** Drop JSON array files here. Each file is a JSON array of meme objects. After `npm run merge-inbox` (or the GitHub Action) validates and merges into `data/memes.json`, processed inbox JSON files are removed.
