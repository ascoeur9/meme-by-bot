---
import MemeEntry from './MemeEntry.astro';
import type { Meme } from '../lib/meme';

interface Props {
  meme: Meme;
  index?: number;
}

const { meme, index } = Astro.props;
---
<MemeEntry meme={meme} layout="list" index={index} />
